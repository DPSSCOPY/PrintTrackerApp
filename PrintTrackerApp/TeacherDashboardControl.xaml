<UserControl x:Class="PrintTrackerApp.TeacherDashboardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="#F0F4F8">
    <UserControl.Resources>
        <DataTemplate x:Key="GradeTemplate">
            <Border Margin="2" CornerRadius="3">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Grade}" Value="A">
                                <Setter Property="Background" Value="#4CAF50"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Grade}" Value="B">
                                <Setter Property="Background" Value="#2196F3"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Grade}" Value="C">
                                <Setter Property="Background" Value="#FFC107"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Grade}" Value="D">
                                <Setter Property="Background" Value="#FF9800"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Grade}" Value="E">
                                <Setter Property="Background" Value="#F44336"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Grade}" Value="Exam">
                                <Setter Property="Background" Value="#9C27B0"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding Grade}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Grade}" Value="C">
                                    <Setter Property="Foreground" Value="Black"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Grade}" Value="D">
                                    <Setter Property="Foreground" Value="Black"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Teacher Print Statistics" FontSize="20" FontWeight="Bold" Foreground="#1F2937" VerticalAlignment="Top" Margin="0,5,20,0"/>
            
            <WrapPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Grid Margin="0,0,10,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Content="Upload Teacher Data" Background="#0078D4" Foreground="White" Padding="15,5" BorderThickness="0" Click="BtnUploadExcel_Click"/>
                    <Grid Grid.Column="1" Margin="5,0,0,0">
                        <Button x:Name="btnRefreshExcel" Content="↻" Background="Transparent" Foreground="#0078D4" FontSize="18" FontWeight="Bold" Padding="5,0" BorderThickness="0" ToolTip="Refresh Excel Data" Visibility="Collapsed" Click="BtnRefreshExcel_Click"/>
                        <Ellipse x:Name="elExcelChangedIndicator" Width="8" Height="8" Fill="Red" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,2,2,0" Visibility="Collapsed" ToolTip="File modified on disk"/>
                    </Grid>
                </Grid>
                
                <Button x:Name="btnUnmatchedFiles" Content="Unmatched Files (0)" Background="#D83B01" Foreground="White" Padding="15,5" Margin="0,0,10,10" BorderThickness="0" Visibility="Collapsed" Click="BtnUnmatchedFiles_Click"/>
                <Button Content="Upload Print History" Background="#107C41" Foreground="White" Padding="15,5" Margin="0,0,10,10" BorderThickness="0" Click="BtnUploadCsv_Click"/>
                <Button x:Name="btnResetData" Content="Reset Data" Background="#D83B01" Foreground="White" Padding="15,5" Margin="0,0,10,10" BorderThickness="0" Visibility="Collapsed" Click="BtnResetData_Click"/>
                <TextBlock x:Name="txtDataSource" Text="Data Source: App Log" VerticalAlignment="Center" Margin="0,0,10,10" Foreground="#666666" FontStyle="Italic"/>
                
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,10" Foreground="#1F2937" FontWeight="SemiBold"/>
                <TextBox x:Name="txtSearch" Width="150" VerticalAlignment="Center" Margin="0,0,10,10" TextChanged="TxtSearch_TextChanged"/>
                
                <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,5,10" Foreground="#1F2937" FontWeight="SemiBold"/>
                <ComboBox x:Name="cmbDateFilter" Width="120" SelectionChanged="CmbDateFilter_SelectionChanged" SelectedIndex="0" Margin="0,0,5,10" VerticalAlignment="Center">
                    <ComboBoxItem Content="Today"/>
                    <ComboBoxItem Content="Custom Range"/>
                </ComboBox>
                <Button Content="⚙️" ToolTip="Manage Custom Date Ranges" Background="Transparent" BorderThickness="0" Padding="5,0" Margin="0,0,5,10" FontSize="16" Click="BtnManageCustomRanges_Click" VerticalAlignment="Center"/>
                
                <Button Content="📅 Schedule Settings" Background="#1E40AF" Foreground="White" Padding="15,5" Margin="0,0,10,10" BorderThickness="0" Click="BtnScheduleSettings_Click" VerticalAlignment="Center"/>
                
                <StackPanel x:Name="spCustomDate" Orientation="Horizontal" Visibility="Collapsed" Margin="0,0,0,10" VerticalAlignment="Center">
                    <DatePicker x:Name="dpStartDate" Width="100" Margin="0,0,5,0" SelectedDateChanged="DpDate_SelectedDateChanged"/>
                    <TextBlock Text="-" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <DatePicker x:Name="dpEndDate" Width="100" SelectedDateChanged="DpDate_SelectedDateChanged"/>
                </StackPanel>
            </WrapPanel>
        </Grid>
        
        <TabControl Grid.Row="1" PreviewMouseWheel="TabControl_PreviewMouseWheel">
            <TabControl.LayoutTransform>
                <ScaleTransform x:Name="uiScale" ScaleX="1" ScaleY="1"/>
            </TabControl.LayoutTransform>
            <TabItem Header="All Data">
                <DataGrid x:Name="dgStats" AutoGenerateColumns="False" CanUserAddRows="False" 
                          Background="White" RowHeight="30" HeadersVisibility="Column" 
                          ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <EventSetter Event="MouseDoubleClick" Handler="DataGrid_MouseDoubleClick_AutoFit"/>
                            <Setter Property="Background" Value="#E5E7EB"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="5,5"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <EventSetter Event="MouseDoubleClick" Handler="DataGrid_MouseDoubleClick_AutoFit"/>
                            <Setter Property="Padding" Value="5,0"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Teacher Name" Binding="{Binding TeacherName}" Width="*" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="ToolTip" Value="{Binding JobsTooltip}" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="Padding" Value="5,0" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Level / Class" Binding="{Binding Level}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Session" Binding="{Binding Session}" Width="100" IsReadOnly="True"/>
                        <DataGridTemplateColumn Header="Grade" Width="60" CellTemplate="{StaticResource GradeTemplate}"/>
                        <DataGridTextColumn Header="Jobs" Binding="{Binding JobCount}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Total Pages" x:Name="colTotalPages" Binding="{Binding TotalPages}" Width="100" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            
            <TabItem Header="FT">
                <Grid x:Name="gridFT">
                    <!-- Columns will be added in code behind -->
                </Grid>
            </TabItem>
            <TabItem Header="PT">
                <Grid x:Name="gridPT">
                </Grid>
            </TabItem>
            <TabItem Header="KH">
                <Grid x:Name="gridKH">
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
