<Window x:Class="PrintTrackerApp.CustomConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm" SizeToContent="Height" MinHeight="180" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True" FontFamily="Noto Sans Khmer, Arial">

    <Window.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2"/>
            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,20,0,0" To="0,0,0,0" Duration="0:0:0.2" DecelerationRatio="0.9"/>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeIn}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border Background="White" CornerRadius="12" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="15" ShadowDepth="5"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border x:Name="borderHeader" Background="#F59E0B" CornerRadius="12,12,0,0" Padding="15,10">
                <Grid>
                    <TextBlock x:Name="txtTitle" Text="Confirm" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                    <Button Content="✕" Foreground="White" Background="Transparent" BorderThickness="0" FontSize="16" FontWeight="Bold" 
                            HorizontalAlignment="Right" VerticalAlignment="Center" Cursor="Hand" Click="BtnNo_Click"/>
                </Grid>
            </Border>

            <!-- Body -->
            <StackPanel Grid.Row="1" Margin="20" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,20">
                    <TextBlock x:Name="txtIcon" Text="⚠️" FontSize="28" Margin="0,0,15,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="txtMessage" Text="Message goes here." 
                               TextWrapping="Wrap" FontSize="14" Foreground="#334155" VerticalAlignment="Center" LineHeight="22" MaxWidth="320"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="btnYes" Content="បាទ/ចាស (Yes)" Height="38" Width="120" Background="#10B981" Foreground="White" FontWeight="SemiBold" FontSize="14" BorderThickness="0" Cursor="Hand" Click="BtnYes_Click" Margin="0,0,15,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button x:Name="btnNo" Content="ទេ (No)" Height="38" Width="120" Background="#EF4444" Foreground="White" FontWeight="SemiBold" FontSize="14" BorderThickness="0" Cursor="Hand" Click="BtnNo_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
