<Window x:Class="PrintTrackerApp.NotificationPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Success" Height="180" Width="380"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True">
    
    <Window.Resources>
        <!-- Fade in animation -->
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,20,0,0" To="0,0,0,0" Duration="0:0:0.3" DecelerationRatio="0.9"/>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeIn}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border Background="White" CornerRadius="12" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="15" ShadowDepth="5"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Background="#10B981" CornerRadius="12,12,0,0" Padding="15,10">
                <Grid>
                    <TextBlock Text="🎉 Print Job Complete!" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                    <Button Content="✕" Foreground="White" Background="Transparent" BorderThickness="0" FontSize="16" FontWeight="Bold" 
                            HorizontalAlignment="Right" VerticalAlignment="Center" Cursor="Hand" Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <!-- Body -->
            <StackPanel Grid.Row="1" Margin="20,15,20,15" VerticalAlignment="Center">
                <TextBlock x:Name="txtMessage" Text="All files in your folder have been successfully printed and moved to Complete Print." 
                           TextWrapping="Wrap" FontSize="14" Foreground="#334155" TextAlignment="Center" LineHeight="20"/>
                
                <Button Content="OK, Great!" Width="120" Height="32" Margin="0,15,0,0"
                        Background="#0EA5E9" Foreground="White" FontWeight="SemiBold" BorderThickness="0"
                        Cursor="Hand" Click="BtnClose_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
