<Window x:Class="PrintTrackerApp.UIInspectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UI Inspector Tool" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="This tool helps find the hidden IDs of buttons in the Printer Properties." TextWrapping="Wrap" Margin="0,0,0,10"/>
        
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <Button Content="1. Click here, then quickly open and focus on the Printer Properties Window (you have 5 seconds)" 
                    Click="BtnDump_Click" Padding="10,5" Background="#0078D4" Foreground="White" FontWeight="Bold"/>
            <TextBlock x:Name="txtCountdown" Text="" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="Bold" Foreground="Red" FontSize="16"/>
        </StackPanel>

        <TextBox x:Name="txtDump" Grid.Row="2" AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" IsReadOnly="True"/>
    </Grid>
</Window>
