Update Version 1.0.31 Features:
- Fixed memory leak on Telegram Bot server by caching the Google Sheets service client and implementing active cache eviction.
- Added SIGTERM/SIGINT signal handling to the bot for graceful termination and zero-downtime redeployments.
- Removed the "Teacher Name / Display Name" setting from the UI and Google Sheets sync since it is no longer displayed.
- Added a "Restart Bot" option to the Telegram custom keyboards to allow users to reset their conversation state easily.

Update Version 1.0.30 Features:
- Enabled "Auto Shutdown PC" feature in settings.
- Added 30-second warning countdown dialog prior to system shutdown, allowing the user to cancel.
- Implemented shutdown execution supporting both "after all prints complete" and "at a scheduled time".
- Fixed settings save/load behavior to retain auto shutdown preferences on application restart.
- Fixed sudden power cutoff log clear bug: Replaced standard file writers for logs, settings, and schedules with atomic safe-writers to prevent file truncation/corruption on sudden PC shutdown.

Update Version 1.0.29 Features:
- Added Level and Session columns to the PT tab in Google Sheets export to match the dashboard layout exactly.
- Made Google Sheets export sorting respect the active sorting selected on the dashboard.

Update Version 1.0.28 Features:
- Fixed columns alignment/per-table layout mismatch in the PT tab to ensure correct grid positioning and cell borders formatting.
- Added custom Google Sheets export start cell/row offset configuration in settings (e.g. A4 or 4) to leave empty rows at the top of the sheets for manual headers.

Update Version 1.0.27 Features:
- Fixed Visibility tab loading: Loaded all teachers from the Excel worksheets directly instead of filtering out teachers without print job activity.
- Fixed skipped teachers in KH tab: Implemented dynamic header detection to avoid skipping the first teacher in the KH list due to hardcoded offsets.
- Filtered out sheet header row labels (like "Teacher Name") from the teacher list.
- Fixed duplicate check logic to include Category, ensuring teachers listed in multiple categories (e.g., both FT and PT) appear in all corresponding tabs.
- Fixed Schedule Settings Level column: FT/KH teachers now show their actual class levels (e.g., G5B, G7A) copied from the dashboard print stats instead of being blank.
- Fixed false No Teach grade: FT/KH teachers with level-specific print data no longer incorrectly show "No Teach" due to a key mismatch between the schedule save key and the grade lookup key.
- Fixed Settings UI behavior for Hidden Teachers: Unchecked/hidden teachers are no longer filtered out of the settings window, allowing them to remain visible in the grid (with unchecked boxes) so they can be re-enabled/unhidden at any time.
- Fixed duplicate teacher rows in the dashboard tabs: Cleaned up schedule keys that contained long document file suffixes (e.g. "G3C-15 copies..."), which previously bypassed duplicate prevention checks because of differing session names. Added auto-migration to cleanly map existing dirty schedule keys to clean levels.

Update Version 1.0.26 Features:
- Added double-click navigation: Double-clicking on a teacher cell in the print statistics dashboard automatically opens the schedules dialog and focuses on that teacher.
- Added persistent filter settings: Preserves the selected custom range or date filter in the schedule settings when closed and reopened.

Update Version 1.0.25 Features:
- Fixed subfolder creation issue in UpdateFileStatusLocation (restricted to standard print statuses).
- Fixed duplicate files count sync in verification panel (aligned with duplicate window regex scanning).
- Optimized dashboard performance (added normalization caching, reduced search iterations, and optimized file reads).
- Added daily duplicate print deduplication to dashboard page counts.
- Added automatic daily duplicate print job highlighting in the main print grid.

Update Version 1.0.24 Features:
- Fixed empty status folder creation bug: Prevented the application from automatically creating empty subdirectories in the watch folder for print jobs when their physical PDF files do not exist (e.g., historical print logs loaded from external CSVs).

Update Version 1.0.23 Features:
- Fixed external CSV saving bug: Editing an external CSV file and saving now writes back to its original path instead of overwriting today's default CSV print log.
- Synchronized Dashboard updating: Data modifications (edits, deletions, cancels, pastes, and undo/redo actions) now immediately trigger a dashboard recalculation.
- Fixed Windows Startup capturing bug: When the application starts minimized/hidden on Windows boot (via the -background argument), all print spooler tracking and SNMP polling services now initialize immediately.
- Optimized Tab switching performance: Offloaded all heavy calculations (exemption matching, grade evaluation, and LCS name matching) to a background thread and implemented DataGrid UI control caching/reuse to achieve instantaneous tab switching with zero lag.
- Restored print detail hover tooltips on teacher names.

Update Version 1.0.22 Features:
- Improved Auto Print error handling: added immediate skip to next available files on printing error without blocking the queue.
- Implemented intelligent retry mechanism: failed files are automatically retried after all unfailed files in the queue have been processed.
- Added 3-retry limit with 1-minute cooldown per file: after 3 consecutive failures, a specific file enters a 60-second cooldown while allowing other files to print normally without stopping Auto Print.
- Fixed Teacher Dashboard external CSV data synchronization so external logs appear consistently on both Print Tracker and Dashboard tabs without merging into default live data.
- Enhanced teacher schedule matching algorithm with strict Level verification and normalization (handling variations like Pre2A1, Pre-2A-I) to prevent mismatched assignment across grade levels.




